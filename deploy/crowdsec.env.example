# CrowdSec LAPI — /etc/log-guardian/crowdsec.env
#   sudo cp deploy/crowdsec.env.example /etc/log-guardian/crowdsec.env
#   sudo chmod 600 /etc/log-guardian/crowdsec.env
#   sudo systemctl enable --now log-guardian-crowdsec-bouncer.timer
CROWDSEC_LAPI_URL=http://127.0.0.1:8081
CROWDSEC_API_KEY=
CROWDSEC_MAX_DECISIONS=50
