# Rust Wasm plugin iskeleti

Wasmtime ile derlemek için (gelecek sürüm):

```bash
cargo install cargo-wasi  # veya wasm32-wasi target
# export fn analyze(ptr, len) -> i32
```

Şimdilik stub `.wasm` dosyalarını kullanın (`../block-sqli.wasm`).
