# Nginx — Log Guardian tam ornek (rate limit + log + proxy)
# Kurulum: sudo bash scripts/prod_edge_setup.sh

include /etc/nginx/snippets/log-guardian.conf;

server {
    listen 80;
    server_name _;

    include /etc/nginx/snippets/log-guardian-server.conf;
}
