# Log Guardian — nginx snippet (log format + rate limit + conn limit)
# Kurulum: include /etc/nginx/snippets/log-guardian.conf;
# Site:    include snippets/log-guardian.conf;  (http {} icinde)

log_format log_guardian '$remote_addr - $remote_user [$time_local] '
    '"$request" $status $body_bytes_sent "$http_referer" '
    '"$http_user_agent" "$http_x_forwarded_for" '
    '"$request_body"';

# Ban gelmeden once nefes — IP basina istek/saniye
limit_req_zone  $binary_remote_addr zone=lg_general:10m rate=30r/s;
limit_req_zone  $binary_remote_addr zone=lg_login:10m   rate=5r/m;
limit_conn_zone $binary_remote_addr zone=lg_conn:10m;

# Cloudflare arkasi (opsiyonel — EDGE_PROTECTION.md)
# set_real_ip_from 173.245.48.0/20;
# real_ip_header CF-Connecting-IP;
