# Log Guardian — static site rate limit (zones: log-guardian-http-inline.conf)
# server {} icinde include edin; proxy backend icin log-guardian-server.conf kullanin.

limit_conn lg_conn 50;
limit_req zone=lg_general burst=60 nodelay;
