---
description: Linux Log Guardian marka, kapsam ve modül rehberi
alwaysApply: true
---

# Log Guardian proje kuralları

## Marka
- Dış metin: **Linux Log Guardian** veya kısaca **Log Guardian**
- Binary/paket: `log-guardian` — "loganalyzer" yalnızca legacy metrik prefix (`loganalyzer_*`)
- Detay: `docs/BRANDING.md`

## Kapsam mesajı
- İlk anlatım: **Core** = nginx 15 dk, log→WAF→kernel ban
- XDR, fleet, Wasm, Copilot LLM = **opsiyonel katman**; "hepsi bir arada platform" dili kullanma
- Enterprise roadmap = uzun vadeli vizyon; quickstart ile çeliştirme

## Kod gezinme
- Modül haritası ve token kuralları: kök `AGENTS.md`
- Tüm repoyu tarama yerine göreve özgü 3–8 dosya oku
- `graphify-out/graph.json` ham halde context'e ekleme

## Grafana
- Dashboard/alert: `grafana-dashboard.json`, `grafana-alerts.json`
- Tenant değişkeni: `$tenant` — sorgularda `tenant_id="$tenant"`

## Laptop operasyon
- Script matrisi: `docs/LAPTOP_OPS.md`
- API: `ensure_api_security.sh` · JWT: `laptop_jwt_setup.sh` · FP prod: `install_fp_trust_prod.sh` · Soak: `laptop_soak_72h.sh`
- Demo parola `DegistirBeni!123` repoda kalir; internet sunucusunda `laptop_harden.sh`

## VM (VirtualBox) — kod değişince hatırlat
C/binary veya script değişikliğinden sonra kullanıcıya VM akışını hatırlat:
`vm_sync_from_host.sh` → (C ise) `vm_build_binary.sh` → `vm_demo_gate.sh` (FAIL=0).
Detay: `docs/LAPTOP_OPS.md` § VM güncelleme · `rsync -a /mnt/lg/` kullanma.

## Geliştirme önceliği (GitHub en son)
1. VM demo gate 0 FAIL · 2. Site · 3. Dashboard 8090 · 4. `.deb` · 5. VM binary pipeline
6. Webhook P2 (ACK/batch) · 7. Attack map · 8. GitHub push/release
